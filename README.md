# Домовой

Единая self-hosted система управления жизнью на основе графа. Заменяет десятки разрозненных приложений — от списка покупок до IT-инфраструктуры — одной моделью данных.

## Содержание

- [Идея](#идея)
- [Что умеет](#что-умеет)
- [Граф: 1 тип узла, 27 рёбер](#граф-1-тип-узла-27-рёбер)
- [Стек](#стек)
- [Архитектура](#архитектура)
- [Структура](#структура)
- [Документация](#документация)

---

## Идея

Всё есть **вещь**. Полка, мотоцикл, задача, рецепт, врач, жалоба, трекинг-номер, сервер, AI-чат, плейбук, дашборд — один тип узла. Смысл задаётся рёбрами между узлами.

```
Задача: ТО мотоцикла
  ├── assigned_to → Папа
  ├── part_of     → Подготовка к сезону
  ├── depends_on  → Купить масло и фильтры
  └── produces    → Запись о ТО (исторический факт)

Сервер: Proxmox-home
  ├── contains    → ВМ: Nextcloud
  ├── contains    → ВМ: Pi-hole
  ├── located_at  → Стойка в кладовке
  └── about       ← Инцидент: диск заполнен

AI-чат: Анализ инцидента
  ├── about       → Сервер: Proxmox-home
  ├── produces    → Задача: заменить диск
  └── inputs      → RAG: последние инциденты из графа
```

Это позволяет моделировать что угодно без новых таблиц и без переключения между приложениями.

## Что умеет

| Область | Возможности |
|---------|-------------|
| **Инвентарь** | Где хранится, кому одолжено, когда куплено, история перемещений |
| **Задачи / GTD** | Подзадачи, зависимости, блокирующий каскад, контекст, энергия |
| **Семья** | Совместные и личные задачи, временные группы, обещания |
| **Медицина** | Приёмы, назначения, направления, цепочка ожидания записи |
| **Юриспруденция** | Обращения, обжалования, fan-out по инстанциям |
| **Платежи** | Периодические шаблоны, история оплат, просрочки |
| **Доставки** | Сложный трекинг, смена перевозчика, разбитые посылки |
| **Обучение** | Курсы, квизы, попытки, AI-адаптация по слабым темам |
| **Документы** | Физические оригиналы + цифровые копии, MinIO-хранилище, OCR |
| **IT-инфраструктура** | Сети, серверы, сервисы, зависимости, инциденты, мультитенант |
| **Дашборды** | ECharts, Mermaid, таблицы, скаляры; микс SurrealQL + Prometheus |
| **AI-чаты** | Единый хаб, RAG из графа, ветвление, параллельные модели, агенты |
| **Голос / Бот** | Telegram-бот, голосовой ассистент, tool use над графом |
| **Плейбуки** | n8n-подобные workflow, условия, циклы, параллельные шаги |
| **Публикации** | Блог, сайт, Telegram-канал — граф как источник истины |
| **Wiki** | Документация, executable-ячейки, cross-references |
| **Торговля** | Витрина, остатки, заказы, доставка, интернет-магазин |
| **Терминал** | Логирование сессий, asciinema vault, bastion datasource |
| **Корзина** | Soft delete, история изменений, история перемещений |
| **Мультиязычность** | `_lang` + `_i18n` на узлах, AI-перевод, human review, stale detection |
| **CRM** | Контакты, клиенты, сделки, воронка продаж, связь с задачами и финансами |

## Граф: 1 тип узла, 27 рёбер

```
contains      — где физически находится / содержит
part_of       — часть чего / подзадача / запуск шаблона
assigned_to   — кто отвечает
depends_on    — задача ждёт другой / сервис зависит от сервиса
about         — касается этой вещи или решения
filed_with    — подано в инстанцию
needs         — нужно купить (quantity, unit)
requires      — плановый расход шаблона
produces      — результат выполнения / публикация
used          — фактический расход
participant   — участник с ролью
located_at    — где происходит событие (started_at, ended_at)
lent_to       — отдано во временное пользование (since)
borrowed_from — взято у человека (since)
triggered_by  — активируется при событии / расписании
expert_in     — эксперт в области
answered      — попытка → вопрос (chosen, correct, points_earned)
promised_to   — кому дано обещание
represents    — цифровая копия → физический оригинал
can_access    — кто имеет доступ (permissions, approval)
related_to    — произвольная связь (label)
references    — ссылка на узел с режимом и снапшотом текста
identified_by — идентифицируется меткой / QR / штрихкодом
linked_to     — физический или логический сетевой линк (schemaless)
invalidates   — изменение узла инвалидирует кэш / сборку
supersedes    — версия заменяет предыдущую (порядок версий; текущая = голова цепочки)
derived_from  — получено из (происхождение/lineage: method, run, at)
```

## Стек

| Слой | Технология |
|------|-----------|
| База данных | [SurrealDB](https://surrealdb.com) — граф, schemaless, SurrealQL, LIVE SELECT |
| Фронтенд | Next.js 15 (App Router), TypeScript |
| UI | Tailwind CSS + shadcn/ui |
| Кэш | TanStack Query |
| Файлы | MinIO (S3-совместимый, self-hosted) |
| Воркеры | Stateless workers — Kubernetes / bare-metal |
| PWA | next-pwa — офлайн, push-уведомления |

## Архитектура

```
SurrealDB  (граф + очереди + LIVE SELECT)
    ↑↓
App Servers (Next.js, stateless, ×N)
Workers     (плейбуки, AI, HTTP, файлы, ×M каждый)
    ↓
MinIO       (файлы, медиа, бинарные данные)
Prometheus  (метрики и телеметрия, опционально)
```

Воркеры конкурируют за задачи через атомарный захват из SurrealDB-очереди. Масштабируются горизонтально без изменений схемы.

## Структура

```
domovoy/
├── docs/                    — документация (см. docs/README.md — индекс и порядок чтения)
│   ├── database.md          — полная схема, сценарии, SurrealQL-запросы (~10 000 строк)
│   ├── comparison.md        — сравнение с существующими решениями
│   ├── kind-audit.md        — канон kind, иерархия категорий, SKOS-словари
│   ├── relation-typing.md   — типизация связей
│   ├── access-control.md    — модель доступа
│   ├── ui-architecture.md   — UI как данные
│   ├── categorization.md    — категоризация как процесс
│   ├── roadmap-inventory.md — роадмап первой вертикали
│   ├── decisions.md         — реестр решений и открытых вопросов
│   └── docs-audit.md        — ревизия документации
├── web/                     — Next.js (в разработке)
├── worker-*/                — воркеры TS/Python (в разработке)
└── scripts/                 — seed, генераторы, бэкап
```

## Документация

Индекс, статусы документов и порядок чтения — [`docs/README.md`](docs/README.md).

- [`docs/database.md`](docs/database.md) — вся схема данных, диаграммы Mermaid, примеры SurrealQL
- [`docs/comparison.md`](docs/comparison.md) — сравнение с Notion, n8n, NetBox, Obsidian и другими
- [`docs/access-control.md`](docs/access-control.md) — модель доступа и маппинг на пользователей / PERMISSIONS SurrealDB
- [`docs/relation-typing.md`](docs/relation-typing.md) — типизация связей через каталог-данные (gradual typing), ответ на критику «schemaless = хаос»
- [`docs/kind-audit.md`](docs/kind-audit.md) — аудит kind в seed, миграция 56→27 ASCII-канонических, иерархия категорий, SKOS-словари
- [`docs/ui-architecture.md`](docs/ui-architecture.md) — UI как данные: универсальный движок на виджетах с контрактами и декларативной композицией из каталога
- [`docs/roadmap-inventory.md`](docs/roadmap-inventory.md) — роадмап первой вертикали (инвентарь + места хранения): 10 фаз × 1–3 дня, прокатывает всю архитектуру
- [`docs/categorization.md`](docs/categorization.md) — как происходит категоризация: 6 моментов × 5 источников × 3 роли; стартуем с пустого каталога, дерево вырастает из кликов
- [`docs/decisions.md`](docs/decisions.md) — реестр всех решений и открытых вопросов (блокеры фазы 1 — там же)
- [`docs/docs-audit.md`](docs/docs-audit.md) — ревизия документации: два поколения архитектуры, противоречия, план приведения к канону
